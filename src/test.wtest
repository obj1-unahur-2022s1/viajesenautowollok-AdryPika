import clientas.*
import oficina.*
import remiseras.*
/*
 * Test: Revisar que habia un test que no pasaba
 * Recorda que para asegurarte que todo funcione bien te conviene hacer muchos test, los que hicieste quedaron cortos
 */

test "Ana Maria esta estable" {

	assert.that(anaMaria.estaEconomicamenteEstable())

}

test "cuanto cobra"{
	
	assert.equals(180, roxana.precioViaje(ludmila,10) )
	assert.equals(200, juana.precioViaje(anaMaria,10) )
	assert.equals(216, gabriela.precioViaje(ludmila,10))
}

test "viajes 2 km"{
	assert.equals(43.20, gabriela.precioViaje(ludmila, 2))
}
test "reemplazos"{
	lucia.reemplazoDe(gabriela)
	
	assert.equals(180 * 1.2, lucia.precioViaje(ludmila, 10))
}

